/**
 * Home route.
 *
 * A route file wires and re-exports; all UI lives in the feature
 * (CLAUDE.md §17). Restructuring the router never touches screen code.
 */
export { HomeScreen as default } from '@/features/weather';
